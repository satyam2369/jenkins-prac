pipeline {
    agent any  // Runs on any available agent
    stages {
        stage('Hello World') {
            steps {
                echo 'Hello, World! Jenkins is running successfully!'
            }
        }
    }
}
